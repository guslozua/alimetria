# Configuración de email para notificaciones
# Descomenta y configura estas variables para habilitar emails

# Gmail (recomendado para pruebas)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu-email@gmail.com
# SMTP_PASS=tu-app-password
# SMTP_FROM_NAME=Alimetria - Sistema de Nutrición

# Otros proveedores
# SMTP_HOST=smtp.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Para Gmail, necesitarás:
# 1. Habilitar autenticación de 2 factores
# 2. Generar una "App Password" específica para Alimetria
# 3. Usar esa App Password en SMTP_PASS

# Si no configuras estas variables, las notificaciones solo funcionarán
# dentro de la aplicación (no se enviarán emails)
