# ALTERNATIVA TEMPORAL: Configuración OAuth2 para Gmail
# Si no encuentras App Passwords, usa esta configuración

# Gmail OAuth2 (más complejo pero más seguro)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=guslozua@gmail.com
# Para OAuth2 necesitarías configurar más parámetros

# ALTERNATIVA: Usar otro proveedor de email
# Outlook (más fácil de configurar)
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu-email@outlook.com
# SMTP_PASS=tu-password-normal

# O usar un servicio como SendGrid, Mailgun, etc.
